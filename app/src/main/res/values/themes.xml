<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.LibraryBottomNav" parent="Theme.AppCompat.DayNight">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <!--  Light mode  -->
    <!--  Screen background if light mode on    -->
    <style name="BACKGROUND" >
        <item name="android:background">#FFFFFF</item>
    </style>

    <!--  Title background if light mode on    -->
    <style name="TITLE" >
        <item name="android:background">#FFFFFF</item>
        <item name="android:textColor">#000000</item>
    </style>

    <!--  Text background if light mode on    -->
    <style name="TEXT" >
        <item name="android:textColor">#000000</item>
    </style>

    <!--  Button background if light mode on    -->
    <style name="BUTTON" >
        <item name="android:background">#000000</item>
        <item name="android:textColor">#FFFFFF</item>
    </style>

    <!--  Button background if light mode on    -->
    <style name="TAB" parent="Widget.Design.TabLayout">
        <item name="tabBackground">@color/white</item> <!-- Background color of each tab -->
        <item name="tabTextColor">@color/black</item> <!-- Default text color of tabs -->
        <item name="tabSelectedTextColor">@color/dark_blue</item> <!-- Text color when a tab is selected -->
        <item name="tabIndicatorColor">@color/black</item> <!-- Color of the indicator below the selected tab -->
    </style>

</resources>